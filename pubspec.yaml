name: dolphin_cli
description: The official dev tools Command Line Interface for the Dolphin Framework for Flutter.
version: 0.2.0
homepage: https://github.com/rkishan516/dolphin_cli
repository: https://github.com/rkishan516/dolphin_cli
topics: [cli, codegen, template, boilerplate]

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  args: ^2.5.0
  freezed_annotation: ^2.4.2
  mockito: ^5.4.4
  mustache_template: ^2.0.0
  path: ^1.9.0
  pubspec_yaml: ^3.1.0
  recase: ^4.0.0
  json_annotation: ^4.9.0
  xdg_directories: ^1.0.4
  pub_updater: ^0.4.0
  mason_logger: ^0.2.15
  cli_completion: ^0.5.0
  scoped_zone: ^0.1.0+1

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.8

  build_runner: ^2.4.11
  freezed: ^2.5.4
  json_serializable: ^6.8.0

executables:
  dolphin:
